Read README.md first. It says where the copy lives, how to check a change (`node scripts/check.mjs`
against `pnpm preview`, plus `scripts/walk.mjs` screenshots to look at), and the site's rules.

When starting the Astro dev server, use background mode: `astro dev --background`, then
`astro dev stop` / `status` / `logs`. Docs: https://docs.astro.build.
